import beatlesConfig from './beatles/site.js';
import friendsConfig from './friends/site.js';

const siteConfigs = {
    beatles: beatlesConfig,
    friends: friendsConfig,
  };
  
  const config = siteConfigs.beatles;
  export default config;
  